# Si-Cuti (React/Vite Version)
Generated by: Bolt.new

## Prerequisites
- Node.js installed

## Setup & Run Instructions
1. Open terminal and navigate to this folder.
2. Install dependencies: `npm install`
3. Run the development server: `npm run dev`
4. Open the provided localhost link in your browser (usually `http://localhost:5173`).

## Notes
Data is persisted using browser `localStorage`. No separate database setup is required.
2. Buat README.md di dalam folder version2-python
Buat file README.md di folder versi 2, lalu isi dengan ini:

Markdown
# Si-Cuti (Python/FastAPI Version)

## Prerequisites
- Python 3.8+ installed

## Setup & Run Instructions
1. Open terminal and navigate to this folder.
2. Install dependencies: `pip install -r requirements.txt`
3. Run the application: `uvicorn main:app --reload`
4. Open your browser and go to: `http://localhost:8000`

## Notes
Data is persisted using SQLite. The database file (`cuti.db`) is automatically generated upon startup.
3. Buat README.md di dalam folder version3-express
Buat file README.md di folder versi 3, lalu isi dengan ini:

Markdown
# Si-Cuti (Node.js/Express Version)

## Prerequisites
- Node.js installed

## Setup & Run Instructions
1. Open terminal and navigate to this folder.
2. Install dependencies: `npm install`
3. Run the server: `node server.js`
4. Open your browser and go to: `http://localhost:3000`

## Notes
Data is persisted in an in-memory array. Restarting the server will reset the data.